/***
 * @author Roar Hoksnes Eriksen
 */

//Klasse for ruter som har en forhaandsutfylt verdi
class SquareWithValue extends Square {

    SquareWithValue(Board board, int value, int dim, int boxPos, int row, int col) {
        super(board, value, dim, boxPos, row, col);
    }
}
